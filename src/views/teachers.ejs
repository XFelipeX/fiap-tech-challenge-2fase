<%- include('index') %>

    <div class="blog-title-div">
      <p class="page-title">Professores</p>
      <p class="action-button" style="width: 205px;">Cadastrar Professores</p>
      <p class="action-button">Voltar</p>
    </div>

    <div class="blog-content">
      <div class="post-box">
      	<p class="teacher-header">Nome</p>
				<hr class="line-style"/>
        <% teachers.forEach(function(teacher) { %>
          <div class="div-display">
            <p class="teacher-name"><%= teacher.name %></p>
            <p class="teacher-button">Editar</p>
            <p class="teacher-button red-button">Excluir</p>
          </div>
        <% }) %>
      </div>
    </div>
  </body>
</html>