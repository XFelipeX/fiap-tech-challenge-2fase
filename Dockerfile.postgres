FROM postgres:latest

ARG POSTGRESDB_USER
ARG POSTGRESDB_ROOT_PASSWORD
ARG POSTGRESDB_DATABASE

ENV POSTGRES_USER=$POSTGRESDB_USER
ENV POSTGRES_PASSWORD=$POSTGRESDB_ROOT_PASSWORD
ENV POSTGRES_DB=$POSTGRESDB_DATABASE

COPY ./src/config/init.sql /docker-entrypoint-initdb.d/